# uv

macOS / Linux

```
curl -LsSf https://astral.sh/uv/install.sh | sh
```

PowerShell

```
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```
```
$env:Path = "C:\Users\gxx_l\.local\bin;$env:Path"
```
```
uv sync
```

# git 
```
git tag -a v0.0.1 -m 'Bind to a port'
git push origin master v0.0.1
git show v0.0.1
```